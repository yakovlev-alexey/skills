# Skills

Public skills live next to this file as directories with `SKILL.md`.
External or local-source skills are described by the dotfiles registry at `../skills.json`.

After making changes in dotfiles, run:

```bash
./scripts/install-skills
```
